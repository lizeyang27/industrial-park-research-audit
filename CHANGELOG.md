# Changelog

本项目采用语义化版本。产品版本与知识卡、语料和中间产物的 schema 或 method 版本分别管理，不能互相替代。

## 1.2.0 — 2026-09-14

外部安装默认完整证据运行版。

### Added

- 可随公开仓库分发的产业园区专家问题先验，覆盖参与方激励、项目阶段、招商落地方式、产业转移摩擦、工业载体参数、政策适用、服务兑现、市场指标、资本结构和复制边界；先验只生成问题，不充当事实证据。
- 公开 F-style 完整工作流：框架优先、本地双轮发现、普通网页核验、反证搜索、完整技术台账、可读报告和零至两条证据约束的新观点。
- 完整审阅包校验器，将草稿、运行资源、联网轨迹、技术台账和用户报告做哈希绑定，并检查逐字原文锚点、证据类型、主张覆盖、关闭链路和前后台发布结论一致性。
- `supplied_inputs` 字节回执：用户提供材料必须逐项通过 `--supplied-input ID=PATH` 读取真实文件并重算 SHA-256，不能仅靠台账自报哈希成为证据。
- `supplied_inputs.input_id` 只接受稳定、非路径式标识；验证动作和关闭决定直接或经计算血缘引用用户材料时，不得早于该材料带时区的 `captured_at`。这只建立台账时序下限，不认证真实采集过程。
- 公开网页 URL 增加 HTTP(S)、主机、端口、转义和凭据的基本语法检查；任意主机上的 `/search`、`/web`、`/s` 配常见查询参数，以及若干已识别搜索服务结果页，均不得晋升为证据。
- 可复算证据使用具名操作数、输入证据引用和受限算术表达式；校验器以 Decimal 安全复算、核对声明结果，并用确定性扰动拒绝已识别的无影响操作数，不执行任意代码。
- 审阅包加入保守资源边界：正式证据最多 200 项，递归计算血缘深度最多 64；超过上限失败关闭并应拆分运行。
- 每项正式证据增加必填且非空的 `fit_target_refs`，把范围适配显式绑定到具体主张或新观点；未晋升或无明确目标的候选只保留在 `discovery_hits`。派生主张增加正文结果与计算证据的数值及百分号绑定。
- `discovery_hits` 增加唯一 ID、问题引用、渠道、候选定位、带时区时间、角色和晋升状态的最小对象校验；CLI 拒绝重复 JSON 对象键并对必填类型错配失败关闭。
- 普通单轮场景的 `framework_first_production_pass`，在草稿已经暴露时保留框架化复核，但明确不把它冒充严格的读稿前实验。

### Changed

- 产业园区审阅默认执行公开完整模式；网络可用且用户没有明确要求离线时，直接核验会改变主旨、发布决定或行动建议的当前事实。
- 前台八项问题上限仅用于可读报告；后台技术台账默认完整保存，不再让输出篇幅限制发现能力。
- 用户报告新增“可继续研究的新观点”，证据不足时允许零条并必须明确说明。
- Skill 默认提示明确要求加载公开先验、生成阅读框架、联网查一手来源并输出双层产物。
- 收紧发布闸门：任何要求外部证据的主张，包括 supporting，只要仍是部分支持、冲突、过期、缺失或不可验证，就必须保留未决路线并阻止可发布；外部问题不能用正文自证，派生数字必须保留外部输入血缘。
- 强制问题回指主张，并校验非空验证动作、关闭谓词、人工复核、常见搜索结果页识别，以及前台结论、问题映射和新观点数量与后台台账一致。
- 前台事项改由 `publication_effect` 决定：必须修改/阻断项强制显示、提示项可选显示、无影响项禁止显示；每项以位置、问题、原因和处理动作四个逐字摘录绑定后台，零项使用固定句。
- 未关闭 `R3` 必须使用 `block_publication` 并对应 `hold`；未关闭 `R2` 至少使用 `revise_before_publish`，必要时可升级为阻断。
- 新观点除结构完整外，至少需要一项范围匹配的外部证据，或带外部血缘且已安全复算的派生证据；正文、公开先验和知识卡不能单独支撑。
- 新观点增加唯一 `viewpoint_id`；前台固定使用五个标签，后台参与方、决策、机制、边界和反证字段必须逐字出现在对应标签值中。
- 问题四锚点与观点五字段的前台逐字命中增加有限的明显否定前缀检查；新观点完整机制字段不得逐字复制草稿。这些只是抗错配、抗复述的机械下限，不证明语义等价或观点真正新颖。
- 任何缺少有效证据路线的外部主张，包括 supporting，都必须保留未决计划、关联活动问题并阻止 `ready`。
- 关闭动作不得晚于关闭决定；在旧快照中自报 `revised`、`deleted` 或 `narrowed` 不再构成关闭，必须对修改后的新草稿快照重跑。
- 问题关闭证据改为逐项覆盖全部关联主张；外部主张逐条要求同目标、范围匹配的外部血缘，派生关闭还须与全部关联派生主张各自指定的计算证据一致。
- 用户报告的七个固定章节必须有实质正文；三级标题只允许用于问题与新观点事项。
- 两个零项固定句必须作为独立完整行输出；否定、引语或长句中的子串不再计为零项标记。

### Boundaries

- 公开 F-style 模式复用的是组织逻辑，不包含正式实验 F 所需的私有先验、来源支持 sidecar 或真实读稿前时间链，不能据此宣称复刻某位导师或私有机构知识。
- 网页核验与校验器能约束来源使用和运行记录，但不能证明模型发现了所有问题，也不能替代行业专家、法律、审计或投资意见。
- `opened_pages`、fit、时间和人工复核标志仍是调用方记录；URL 语法和搜索页识别只覆盖已编码的机械边界。操作数扰动也不是完整符号依赖证明。字节重哈希、受限算术复算、否定/复制词面检查和前后台逐字绑定均不证明来源真实、输入摘录正确、网页角色完整、真人身份、两层文字语义等价或观点新颖。

## 1.1.1 — 2026-09-12

普通用户可读输出修订版。

### Added

- 固定的中文用户报告结构：发布结论、文章优点、发布前事项、修改方法、继续核验选择和未验证范围。
- 隐私最小化的用户报告校验器，阻止内部编码、机器字段、英文工作流状态和过长问题清单进入主报告。

### Changed

- 正式交付改为双层：默认用户报告负责可读决策，技术台账单独负责评测、追溯与人工关闭。
- A/B/C/D/E/F 对照统一用户报告模板，避免展示方式差异污染方法比较。

### Boundaries

- 可读性校验只检查结构与泄漏不变量，不判断行业结论、证据质量或审阅准确率。
- 技术编码没有删除；它们继续保存在单独技术记录或用户明确要求的技术附录中。

## 1.1.0 — 2026-09-12

私有经验来源支持与读稿前框架版本。

### Added

- 私有经验来源/支持 sidecar：记录 `origin_trace` 与声明但未核验的独立来源线索，并与既有先验问题清单、授权和来源快照哈希绑定。
- 保守准入规则：已有导师或会议经验在没有独立材料时保持 `origin_trace_only`；即使声明了独立线索，也保持 `declared_not_verified`，不自动提升为事实证据。
- 读稿前 `ReadingFrameworkManifest`：将相同的原子问题组织成研究问题和类型化的定义边界、变量、参与方决策、机制链、替代解释、预期证据与证伪节点，再绑定后续草稿哈希。
- `IRA-ABCDEF-V1.1` 配对评估协议与确定性评分接口，用于分别测量来源支持和框架综合的主效应、交互、成本与安全失败。
- 完全合成的来源支持、框架和因子评估夹具，以及对应的篡改、越权、时序、预算与证据边界测试。
- 结构化 `TopicBriefEnvelope`，使框架输入的研究问题、范围与边界也进入读稿前哈希和时间链。

### Changed

- 公开定位锚定产业园区这一首个真实垂直场景，同时保留 `$industry-research-audit` 作为可迁移审计内核；新增不含现实主体事实的产业园区决策研究框架和三层架构说明。
- 修复公开 `PriorQuestionManifest` schema 落后于运行时的问题；当前 schema 与 `0.2` 字符预算产物一致，运行时仍能读取受支持的旧版清单。
- 私有分层语料的人审接受状态统一为 `accepted`，避免与公开 L3 晋升闸门的状态枚举漂移。
- `SKILL.md` 增加按需路由；详细来源血缘、框架和评测协议继续放在 references，真实私有材料不进入上下文或发布包。
- 来源支持构建器可在显式提供本地根目录时逐个重算来源文件 SHA-256；缺失、越界或哈希失配均失败关闭，并与仅信任清单声明的状态分开记录。
- 阅读框架在冻结、绑定和校验三个阶段都重新检查当前授权；撤权、过期、授权快照变化或不允许抽象模型上下文时失败关闭。
- 因子评分器将真实输入限制到私有输出目录，默认隐藏案例标识，区分共同结果指标与组别过程指标，并把 token 明确标为调用方报告值。

### Boundaries

- 来源可追溯只说明经验如何形成；它不证明经验的行业内容，也不把导师批注、会议发言或同源文章变成目标文章的事实证据。
- C 组已有读稿前原子问题；E/F 新增的是结构化框架综合。历史 v1.0 开发分数不直接迁移为 v1.1 的因素效果。
- 本版本的工程和合成回归不能证明私有知识、支持数据或框架已在真实未见稿上形成稳定增量，也不构成模型训练或专家复刻。

### Compatibility

- 现有 Knowledge Card `0.2`、`knowledge_base.py prior` 和 `prior_trace.py` 工作流保持可用。
- 新增支持 sidecar 与阅读框架均为可选私有运行产物；升级公开 Skill 不覆盖私有知识库或历史运行记录。

## 1.0.0 — 2026-09-09

首个公开候选版本。

### Added

- 中文产业研究稿的推理审计、风险分级与买方研究命题重构入口。
- 相互独立的风险后果 `R1`–`R3`、验证难度 `V1`–`V3` 和文本可发现性 `T1`–`T3`。
- 文档结构抽取、版本比较、分阶段 token 区间估算、知识卡治理、语料校验、容量审计和公开发布预检工具。
- DOCX 提取仅报告图片数量而不复制图片二进制；分层语料构建器支持显式语料库与授权 ID，并可在保留既有记录的前提下追加批次。
- 分层语料校验器向后兼容单快照，并支持由多个不可变快照组成的追加式私有语料库；同一逻辑文章的多个不可变来源版本必须形成显式、无分叉的前后继链。
- 完全合成的示例、确定性单元测试、隐私边界和可发布来源声明。
- GitHub Actions 持续集成，在 Python 3.10 与 3.12 上运行确定性测试、公开边界预检和容量审计。
- 私有知识卡的授权、时效、先验固定、证据边界和人工关闭约束。

### Boundaries

- 本版本是本地、人机协同的 Codex Skill，不是自动事实核验服务、法律或审计意见、投资建议或生产 SaaS。
- 公开仓库不包含真实文章、内部资料、人工批注、会议材料、数据库导出或私有知识库。
- 当前测试证明的是公开脚本和仓库约束在指定合成案例上的行为，不构成模型总体准确率结论。

### Compatibility

- Python 3.10 或更高版本。
- 核心脚本只使用 Python 标准库；PDF 文本抽取可选安装 `pypdf`。
- 发布维护者运行 Codex Skill Creator 的快速校验时需要开发依赖 `PyYAML`。
- 可选运行依赖与开发依赖分别记录在 `requirements-optional.txt` 和 `requirements-dev.txt`。
